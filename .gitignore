.DS_Store
.vscode
cmd/server/server
node_modules
*.code-workspace
.idea/encodings.xml
.idea/misc.xml
.idea/modules.xml
.idea/vcs.xml
.idea/workspace.xml
liwords.iml
.idea/

*.ipynb_checkpoints/
scripts/batchregister/*.csv
__pycache__
scripts/**/*-linux-amd64

data/lexica/words/*.txt
local.env
.env
.env.production

cmd/convert-pb/convert-pb
liwords-ui/public/wasm/macondo.wasm

*.out

reporting/.env
reporting/_results/
reporting/dbhub.toml

# Jupyter notebook checkpoints (never commit)
**/.ipynb_checkpoints/
# Local Python venv for reporting notebooks
reporting/.venv/
# bestbot generated chart artifacts; keep only the queries/notebooks
reporting/bestbot/*.jpg
# never commit query result data snapshots anywhere under reporting/
reporting/**/*.csv

# Compiled binaries and local scratch data. These were committed by accident
# once (a `git add -A` that swept up an untracked working tree) and cost the
# history ~285 MB; the entries are here so it cannot happen again.
gameplay.test
gamehistories
find-corrupted-games
find-pre-archival-games
cmd/profile-game-load/profile-game-load
cmd/repair-force-finished/repair-force-finished
cmd/league-tester/test_users*.json
*.test
