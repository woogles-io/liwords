export NATS_URL=nats://localhost:4222
export TEST_DB_HOST=localhost
export TEST_DB_PREFIX=liwords_test
export DB_NAME=liwords
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=pass
export DB_SSL_MODE=disable

# Make sure these match the path where your code is stored:
export LEXICON_PATH=$HOME/code/liwords/data/lexica
export STRATEGY_PARAMS_PATH=$HOME/code/macondo/data/strategy
export LETTER_DISTRIBUTION_PATH=$HOME/code/liwords/data/letterdistributions
export MACONDO_DATA_PATH=$HOME/code/liwords/data
export DB_MIGRATIONS_PATH=file://$HOME/code/liwords/db/migrations

export REDIS_URL=redis://localhost:6379/1
export SECRET_KEY=jqxztripletriple
export DEBUG=1
export REGISTRATION_CODE=foobar
export ARGON_MEMORY=1024

# Run database migrations when the API server starts up.
export RUN_MIGRATIONS=1
# Create new users pre-verified instead of requiring an email verification
# link, and print outgoing emails to stdout instead of sending them via SES.
export SKIP_EMAIL_VERIFICATION=1
export EMAIL_DEBUG=true